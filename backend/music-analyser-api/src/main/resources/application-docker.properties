# CONFIGURATION SP�CIFIQUE DOCKER

spring.datasource.url=jdbc:postgresql://postgres:5432/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

clickhouse.url=jdbc:clickhouse://clickhouse:8123/default?user=${DB_USER}&password=${DB_PASSWORD}&compress=0

# Optimisation pour le d�veloppement (Hot Reload)
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true